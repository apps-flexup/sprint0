import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { text } from '@storybook/addon-knobs'
import { action } from '@storybook/addon-actions'
import FvCreateButton from './index.vue'

<Meta title='Atoms/FvCreateButton' component={ FvCreateButton } />

# FvCreateButton

<Story name='Common' height='150px'>{{
  props: {
    label: {
      default: text('Label', 'Create', 'Slots')
    }
  },
  methods: {
    clicked: action('clicked')
  },
  components: { FvCreateButton },
  template: `<FvCreateButton
    @button:click="clicked"
  >
    {{ label }}
  </FvCreateButton>`
}}</Story>

## ArgsTable

<ArgsTable of={ FvCreateButton } />
