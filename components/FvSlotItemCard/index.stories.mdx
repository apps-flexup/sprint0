import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { number, color} from '@storybook/addon-knobs'
import { action } from '@storybook/addon-actions'
import { avatarsAvailable, defaultAvatar } from '@/.storybook/knobs_data/avatars'
import FvSlotItemCard from './index.vue'

<Meta title='Atoms/FvSlotItemCard' component={ FvSlotItemCard } />

# FvSlotItemCard

<Canvas>
<Story name='Common' height='150px'>{{
  components: { FvSlotItemCard },
  props: {
    knobCardWidth: {
      default: number('Card width', 300, {}, 'Props')
    },
    knobCardHeight: {
      default: number('Height', 370, {}, 'Props')
    },
    knobFavoriteColor: {
      default: color('Favorite color', 'black', 'Props')
    }
  },
  methods: {
    cardClicked: action('card clicked'),
    favoriteClicked: action('favorite clicked')
  },
  template: `<FvSlotItemCard
    :cardWidth="knobCardWidth"
    :cardHeight="knobCardHeight"
    :favoriteColor="knobfavoriteColor"
    @card:clicked="cardClicked"
    @favorite:clicked="favoriteClicked"
  >
  </FvSlotItemCard>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvSlotItemCard } />
