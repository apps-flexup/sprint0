import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { select } from '@storybook/addon-knobs'
import { spaceTitle, defaultSpaceTitle, } from '@/.storybook/knobs_data/menusAvailable'
import FvMenu from './index.vue'

<Meta title='Molecules/FvMenu' component={ FvMenu } />

# FvMenu

<Canvas>
<Story name='Common' height='150px'>{{
  components: { FvMenu },
  props: {
    knobSpace: {
      default: select('Space', spaceTitle, defaultSpaceTitle, 'Props')
    }
  },
  template: `<FvMenu
    :space="knobSpace"
  > 
  </FvMenu>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvMenu } />
