import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import { spaces, defaultSpace } from '@/.storybook/data/menusAvailable'
import FvNavBar from './index.vue'

<Meta title='Molecules/FvNavBar' component={ FvNavBar } argTypes={{
  title: { control: 'text' },
  space: {
    control: {
      type: 'select',
      options: spaces
    }
  }
}}/>

# FvNavBar

<Canvas>
<Story name='Common' height='150px' args={{
  title: 'Navbar title',
  space: defaultSpace
}}>{{
  props: {
    title: {},
    space: {}
  },
  components: { FvNavBar },
  template: `<FvNavBar
    :title="title"
    :space="space"
  >
  </FvNavBar>`
}}</Story>
</Canvas>

## Props

<Props of={ FvNavBar } />
