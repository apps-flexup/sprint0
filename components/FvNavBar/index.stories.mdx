import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { text, select } from '@storybook/addon-knobs'
import { spaceTitle, defaultSpaceTitle} from '@/.storybook/knobs_data/menusAvailable'
import FvNavBar from './index.vue'

<Meta title='Molecules/FvNavBar' component={ FvNavBar } />

# FvNavBar

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobTitle: {
      default: text('Title', 'Navbar title', 'Slots')
    },
    knobSpaceTitle: {
      default: select('spaceTitle', spaceTitle, defaultSpaceTitle, 'Props')
    }
  },
  components: { FvNavBar },
  template: `<FvNavBar
    :title="knobTitle"
    :spaceTitle="knobSpaceTitle"
  >
  </FvNavBar>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvNavBar } />
