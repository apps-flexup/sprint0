import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { text } from '@storybook/addon-knobs'
import FvNavBar from './index.vue'

<Meta title='Molecules/FvNavBar' component={ FvNavBar } />

# FvNavBar

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobTitle: {
      default: text('Title', 'Navbar title', 'Slots')
    }
  },
  components: { FvNavBar },
  template: `<FvNavBar
    :title="knobTitle"
  >
  </FvNavBar>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvNavBar } />
