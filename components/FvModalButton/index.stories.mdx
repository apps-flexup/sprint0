import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import FvModalButton from './index.vue'

<Meta title='Atoms/FvModalButton' component={ FvModalButton } argTypes={{
  text: { control: 'text' }
}}/>

# FvModalButton

<Canvas>
<Story name='Common' height='150px' args={{
  text: 'Modal button'
}}>{{
  props: {
    text: {}
  },
  components: { FvModalButton },
  methods: {
    click: action('click')
  },
  template: `<FvModalButton
    @button:click="click"
  >
    <template v-slot>
      {{ text }}
    </template>
  </FvModalButton>`
}}</Story>
</Canvas>

## Props

<Props of={ FvModalButton } />
