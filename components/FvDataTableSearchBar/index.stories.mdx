import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import { text, boolean } from '@storybook/addon-knobs'
import FvDataTableSearchBar from './index.vue'

<Meta title='Atoms/FvDataTableSearchBar' component={ FvDataTableSearchBar } />

# FvDataTableSearchBar

<Canvas>
<Story name='Common' height='150px'>{{
  components: { FvDataTableSearchBar },
  props: {
    knobLabel: {
      default: text('Label', 'Label', 'Props')
    }
  },
  components: { FvDataTableSearchBar },
  methods: {
    searchChanged: action('Search changed')
  },
  template: `<FvDataTableSearchBar
    :label="knobLabel"
    @dataTableSearchBar:changed="searchChanged"
  >
  </FvDataTableSearchBar>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvDataTableSearchBar } />
