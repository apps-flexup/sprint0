import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { text, select, number } from '@storybook/addon-knobs'
import { isoDateKnob } from '@/.storybook/knobs_data/isoDateKnob'
import { avatarsAvailable, defaultAvatar } from '@/.storybook/knobs_data/avatars'
import { options } from '@/.storybook/knobs_data/ratings'
import FvReview from './index.vue'

<Meta title='Molecules/FvReview' component={ FvReview } />

# FvReview

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobAvatar: {
      default: select('Avatar', avatarsAvailable, defaultAvatar, 'Props')
    },
    knobUsername: {
      default: text('Username', 'toto', 'Props')
    },
    knobDate: {
      default: isoDateKnob('Date', new Date(2020, 9, 20), 'Props')
    },
    knobReview: {
      default: text('Review', 'Super produit ! Je recommande', 'Props')
    },
    knobRating: {
      default: number('Rating', 4, options, 'Props')
    }
  },
  components: { FvReview },
  template: `<FvReview
    :avatar="knobAvatar"
    :username="knobUsername"
    :date="knobDate"
    :review="knobReview"
    :rating="knobRating"
  >
  </FvReview>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvReview } />
