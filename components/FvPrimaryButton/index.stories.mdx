import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { text } from '@storybook/addon-knobs'
import { action } from '@storybook/addon-actions'
import FvPrimaryButton from './index.vue'

<Meta title='Atoms/FvPrimaryButton' component={ FvPrimaryButton } />

# FvPrimaryButton

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    label: {
      default: text('Label', 'Create', 'Slots')
    }
  },
  methods: {
    clicked: action('clicked')
  },
  components: { FvPrimaryButton },
  template: `<FvPrimaryButton
    @button:click="clicked"
  >
    {{ label }}
  </FvPrimaryButton>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvPrimaryButton } />
