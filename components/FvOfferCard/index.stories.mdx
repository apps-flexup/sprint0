import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { number, boolean, text, select } from '@storybook/addon-knobs'
import { imageAvailable, defaultImage} from '@/.storybook/knobs_data/imageAvailable'
import { unitsAvailable, defaultUnit, dimensionsAvailable, defaultDimension} from '@/.storybook/knobs_data/unitsAvailable'
import { options } from '@/.storybook/knobs_data/ratings'
import { action } from '@storybook/addon-actions'
import FvOfferCard from './index.vue'

<Meta title='Molecules/FvOfferCard' component={ FvOfferCard } />

# FvOfferCard

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobRating: {
      default: number('Rating', 4, options, 'Props')
    },
    knobTitle: {
      default: text('Title', 'Article storybook', 'Props')
    },
    knobPrice: {
      default: number('Price', 15, {}, 'Props')
    },
    knobAvis: {
      default: number('nbrAvis', 3, {}, 'Props')
    },
    knobUnit: {
      default: select('Unit', unitsAvailable, defaultUnit, 'Props')
    },
    knobDimension: {
      default: select('Dimension', dimensionsAvailable, defaultDimension, 'Props')
    },
    knobImage: {
      default: select('Image', imageAvailable, defaultImage, 'Props')
    },
    knobWidth: {
      default: number('Width', 300, {}, 'Props')
    },
    knobHeightImg: {
      default: number('height img', 260, {}, 'Props')
    },
    knobHeightCard: {
      default: number('height card', 370, {}, 'Props')
    }
  },
  components: { FvOfferCard },
  methods: {
    clickedHeart: action('heart click'),
    clickedCard: action('card click'),
    clickedShop: action('shop click'),
    clickedLook: action('look click')
  },
  template: `<FvOfferCard
    :rating="knobRating"
    :article="knobTitle"
    :price="knobPrice"
    :nbrAvis="knobAvis"
    :unit="knobUnit"
    :dimension="knobDimension"
    :img="knobImage"
    :width="knobWidth"
    :heightImg="knobHeightImg"
    :height="knobHeightCard"
    @offerCard:heartClicked="clickedHeart"
    @offerCard:lookClicked="clickedLook"
    @offerCard:shopClicked="clickedShop"
    @offerCard:clicked="clickedCard"
  >
  </FvOfferCard>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvOfferCard } />
