import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import { imagesAvailable, defaultImage } from '@/.storybook/data/imagesAvailable'
import { unitsAvailable, defaultUnit, dimensionsAvailable, defaultDimension } from '@/.storybook/data/unitsAvailable'
import { action } from '@storybook/addon-actions'
import FvOfferCard from './index.vue'

<Meta title='Molecules/FvOfferCard' component={ FvOfferCard } argTypes={{
  rating: {
    control: {
      type: 'range',
      min: 0,
      max: 5,
      step: 0.5
    }
  },
  title: { control: 'text' },
  price: { control: 'number' },
  currency: { control: 'text' },
  nbReviews: { control: 'number' },
  unit: {
    control: {
      type: 'select',
      options: unitsAvailable
    }
  },
  dimension: {
    control: {
      type: 'select',
      options: dimensionsAvailable
    }
  },
  image: {
    control: {
      type: 'select',
      options: imagesAvailable
    }
  },
  heightImg: { control: 'number' }
}}/>

# FvOfferCard

<Canvas>
<Story name='Common' height='150px' args={{
  rating: 4,
  title: 'Article storybook',
  price: 15,
  currency: 'EUR',
  nbReviews: 3,
  unit: defaultUnit,
  dimension: defaultDimension,
  image: defaultImage,
  heightImg: 260
}}>{{
  props: {
    rating: {},
    title: {},
    price: {},
    currency: {},
    nbReviews: {},
    unit: {},
    dimension: {},
    image: {},
    heightImg: {},
  },
  components: { FvOfferCard },
  methods: {
    likeClicked: action('like clicked'),
    cardClicked: action('card clicked'),
    buyClicked: action('buy clicked'),
    detailsClicked: action('details clicked')
  },
  template: `<FvOfferCard
    :rating="rating"
    :article="title"
    :price="price"
    :currency="currency"
    :nbReviews="nbReviews"
    :unit="unit"
    :dimension="dimension"
    :img="image"
    :heightImg="heightImg"
    @offerCard:likeClicked="likeClicked"
    @offerCard:detailsClicked="detailsClicked"
    @offerCard:buyClicked="buyClicked"
    @offerCard:clicked="cardClicked"
  >
  </FvOfferCard>`
}}</Story>
</Canvas>

## Props

<Props of={ FvOfferCard } />
