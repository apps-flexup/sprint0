import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { select } from '@storybook/addon-knobs'
import { statusAvailable, defaultValue } from '@/.storybook/knobs_data/statusAvailable'
import FvStatusProgress from './index.vue'

<Meta title='Atoms/FvStatusProgress' component={ FvStatusProgress } />

# FvStatusProgress

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobStatus: {
      default: select('Status', statusAvailable, defaultValue, 'Props')
    }
  },
  components: { FvStatusProgress },
  template: `<FvStatusProgress
    :status="knobStatus"
  >
  </FvStatusProgress>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvStatusProgress } />
