import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import { statusAvailable, defaultValue } from '@/.storybook/data/statusAvailable'
import FvStatusProgress from './index.vue'

<Meta title='Atoms/FvStatusProgress' component={ FvStatusProgress } argTypes={{
  status: {
    control: {
      type: 'select',
      options: statusAvailable
    }
  }
}}/>

# FvStatusProgress

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    status: {
      default: defaultValue
    }
  },
  components: { FvStatusProgress },
  template: `<FvStatusProgress
    :status="status"
  >
  </FvStatusProgress>`
}}</Story>
</Canvas>

## Props

<Props of={ FvStatusProgress } />
