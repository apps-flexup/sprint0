import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import { text, boolean } from '@storybook/addon-knobs'
import FvPriceField from './index.vue'

<Meta title='Atoms/FvPriceField' component={ FvPriceField } />

# FvPriceField

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobLabel: {
      default: text('Label', 'Label', 'Props')
    },
    knobOutlined: {
      default: boolean('Outlined', true, 'Props')
    }
  },
  components: { FvPriceField },
  methods: {
    priceChanged: action('Price changed')
  },
  template: `<FvPriceField
    :label="knobLabel"
    :outlined="knobOutlined"
    @price:changed="priceChanged"
  >
  </FvPriceField>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvPriceField } />
