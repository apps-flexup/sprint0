import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import { text, boolean } from '@storybook/addon-knobs'
import FvVatField from './index.vue'

<Meta title='Atoms/FvVatField' component={ FvVatField } />

# FvVatField

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobLabel: {
      default: text('Label', 'Label', 'Props')
    },
    knobOutlined: {
      default: boolean('Outlined', true, 'Props')
    }
  },
  components: { FvVatField },
  methods: {
    vatChanged: action('VAT changed')
  },
  template: `<FvVatField
    :label="knobLabel"
    :outlined="knobOutlined"
    @vat:changed="vatChanged"
  >
  </FvVatField>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvVatField } />
