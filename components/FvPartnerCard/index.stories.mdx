import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { select } from '@storybook/addon-knobs'
import { partnersAvailable, defaultValue } from '@/.storybook/knobs_data/partnersAvailable'
import FvPartnerCard from './index.vue'

<Meta title='Atoms/FvPartnerCard' component={ FvPartnerCard } />

# FvPartnerCard

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobPartnerId: {
      default: select('Partner', partnersAvailable, defaultValue, 'Props')
    }
  },
  components: { FvPartnerCard },
  template: `<FvPartnerCard
    :partnerId="knobPartnerId"
  >
  </FvPartnerCard>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvPartnerCard } />
