import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { select } from '@storybook/addon-knobs'
import { category, defaultCategory, menus, defaultMenu } from '@/.storybook/knobs_data/menusAvailable'
import FvTitleMenu from './index.vue'

<Meta title='Atoms/FvTitleMenu' component={ FvTitleMenu } />

# FvTitleMenu

<Canvas>
  <Story name='Common' height='150px'>
    {{
      components: { FvTitleMenu },
      props: {
        knobCategory: {
          default: select('Category', category, defaultCategory, 'Props')
        },
        knobMenu: {
          default: select('Menu', menus, defaultMenu, 'Props')
        }
      },
      template: `<FvTitleMenu
        :categoryName='knobCategory'
        :menuName='knobMenu'
      >
      </FvTitleMenu>`
    }}
  </Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvTitleMenu } />
