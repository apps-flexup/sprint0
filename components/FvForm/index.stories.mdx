import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { select } from '@storybook/addon-knobs'
import { actions, defaultAction } from '@/.storybook/knobs_data/actions'
import { forms, defaultForm } from '@/.storybook/knobs_data/forms'
import FvForm from './index.vue'

<Meta title='organism/FvForm' component={ FvForm } />

# FvForm

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobAction: {
      default: select('action', actions, defaultAction, 'Props')
    },
    knobForm: {
      default: select('form', forms, defaultForm, 'Props')
    }
  },
  components: { FvForm },
  template: `<FvForm
    :action=knobAction
    :form=knobForm
  >
  </FvForm>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvForm } />
