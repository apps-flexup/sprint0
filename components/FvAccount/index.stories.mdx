import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { text, select } from '@storybook/addon-knobs'
import FvAccount from './index.vue'
import { avatarsAvailable, defaultAvatar } from '@/.storybook/knobs_data/avatars'

<Meta title='Molecules/FvAccount' component={ FvAccount } />

# FvAccount

<Canvas>
<Story name='Common' height='150px'>{{
  props: {
    knobName: {
      default: text('Name', 'fred', 'Props')
    },
    knobAvatar: {
      default: select('Avatar', avatarsAvailable, defaultAvatar, 'Props')
    }
  },
  components: { FvAccount },
  template: `<FvAccount
    :name="knobName"
    :avatar="knobAvatar"
  >
  </FvAccount>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvAccount } />
