import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { select, text, array } from '@storybook/addon-knobs'
import { categories, defaultCategory, menus, defaultMenu, fakeList} from '@/.storybook/knobs_data/menusAvailable'
import FvSubMenu from './index.vue'

<Meta title='Molecules/FvSubMenu' component={ FvSubMenu } />

# FvSubMenu

<Canvas>
<Story name='Common' height='150px'>{{
  components: { FvSubMenu },
  props: {
    knobCategory: {
      default: select('Categories', categories, defaultCategory, 'Props')
    },
    knobMenu: {
      default: array('Menus', fakeList, ',', 'Props')
    },
    knobTitle: {
      default: text('Title', 'test', 'Props')
    }
  },
  template: `<FvSubMenu
    :categoryName='knobCategory'
    :findMenu='knobMenu'
    :title='knobTitle'
  >
  </FvSubMenu>`
}}</Story>
</Canvas>

## ArgsTable

<ArgsTable of={ FvSubMenu } />
