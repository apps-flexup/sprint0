import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { select } from '@storybook/addon-knobs'
import { spaces, defaultSpace } from '@/.storybook/knobs_data/spacesAvailable'
import FvSpaceMenu from './index.vue'

<Meta title="Molecules/FvSpaceMenu" component={FvSpaceMenu} />

# FvSpaceMenu

<Canvas>
  <Story name="Common" height="150px">
    {{
      components: { FvSpaceMenu },
      props: {
        knobSpace: {
          default: select('Space', spaces, defaultSpace, 'Props')
        }
      },
      template: `<FvSpaceMenu
    :seller='knobSpace'
    > 
  </FvSpaceMenu>`
    }}
  </Story>
</Canvas>

## ArgsTable

<ArgsTable of={FvSpaceMenu} />
