export default {
  locale(state) {
    const res = state.locale
    return res
  },
  locales(state) {
    const res = state.locales
    return res
  },
  formesJuridiques(state) {
    const res = state.formesJuridiques
    return res
  }
}
