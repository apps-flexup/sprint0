export default {
  set(state, items) {
    state.items = items
  },
  setContractSteps(state, items) {
    state.contracts = items
  },
  setContactSteps(state, items) {
    state.contacts = items
  }
}
