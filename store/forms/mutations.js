export default {
  set(state, items) {
    state.items = items
  },
  setContractSteps(state, items) {
    state.contracts = items
  },
  setPartnerSteps(state, items) {
    state.partners = items
  }
}
